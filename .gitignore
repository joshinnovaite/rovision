.vscode/
.DS_Store
__pycache__/
*-checkpoint.ipynb
.venv
*.egg*
build/*
_C.*
outputs/*
checkpoints/*.pt
demo/backend/checkpoints/*.pt

# Local test footage — keep large video files out of the fork (all domains)
test_footage/
test_footage_*/

# Transient frame dumps from the labelling tool (labels.json IS kept)
label_review/
*.bak
*.bak.json
rovision/labelling/labeller_error.log

# Per-domain golden-set backups (the live labels_*.json ARE kept)
rovision/labelling/labels_*.bak.json
rovision/labelling/*.mixed.*.json

# Transient bundle downloads pulled back from Google Drive
drive-download-*/

# Amplified dataset (frames + labels) — durable copy lives in Google Drive
rovision/dataset/

# Demo app — pre-computed bundles (large clips), generated SQLite DB, build output.
# Durable bundle copies live on Google Drive; clips are large (~75MB each).
app/backend/data/
app/frontend/node_modules/
app/frontend/dist/
