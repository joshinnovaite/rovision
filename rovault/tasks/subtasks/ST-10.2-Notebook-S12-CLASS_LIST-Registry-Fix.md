# ST-10.2: Notebook §12 CLASS_LIST Registry Fix

Parent: [[BOW-10-E1-Phase-0-Proof-Of-Capability]]
Status: done
Progress: done — §12's `CLASS_LIST` was still hardcoded to the 15 subsea classes; repointed at
`DOMAINS[DOMAIN]['all_classes']` + registry colours. Completes DR-013's single-source-of-truth migration
for that cell; required for any non-subsea domain (E1 included) to render in §12.

## Preconditions
- The registry is the single source of truth for taxonomy/colours
  ([[DR-013-Multi-Domain-Registry-And-Inventory-Mode]]); §12 was a straggler still hardcoded to subsea.

## Steps
1. In §12 of [[subsea_defect_demo.ipynb]], replace the hardcoded 15-class `CLASS_LIST` with
   `DOMAINS[DOMAIN]['all_classes']`.
2. Source overlay colours from the registry rather than a local map.
3. Verify §12 renders correctly for a non-subsea `DOMAIN`.

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` — §12 verify cell.

## Acceptance
- §12 renders the active domain's classes/colours from the registry for any `DOMAIN`, not just subsea.

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — single-source-of-truth migration this completes.
- [[DR-016-E1-Domain-As-Inventory-Mode-Phase-0]] — the fix was required for E1 to render.
- [[subsea_defect_demo.ipynb]] — the §12 fix site.
