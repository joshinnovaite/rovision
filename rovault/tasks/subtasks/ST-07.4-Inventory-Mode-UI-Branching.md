# ST-07.4: Inventory-Mode UI Branching

Parent: [[BOW-07-Multi-Domain-App-Architecture]]
Status: done
Progress: done — `mode`-conditional dashboard/header/library tiles/overlay/timeline; `computeClassCounts` added; inventory treats all classes as primary (nothing dimmed). Typechecks + builds.

## Preconditions
- Domain state (`mode`) is available in stores ([[ST-07.3-Frontend-Domain-State-And-Sidebar-Toggle]]).
- The dashboard, video header, overlay, and playback bar from prior BOWs exist.

## Steps
1. `lib/severity.ts`: add `computeClassCounts` (component-count rollup for inventory mode).
2. `screens/DashboardScreen.tsx`: branch on `mode` — defect shows severity/flags; inventory shows component counts.
3. `components/playback/VideoHeader.tsx`: suppress severity/flag chrome in inventory mode.
4. `components/playback/OverlayLayer.tsx` + `PlaybackBarCard.tsx`: in inventory mode treat **all** classes as primary so nothing is dimmed.
5. `components/playback/AssetDropdown.tsx`: domain-appropriate asset lenses.
6. `components/upload/ParameterModal.tsx`: domain-aware class/parameter options.
7. `styles/components.css`: any mode-specific styling.

## Files
- `app/frontend/src/lib/severity.ts`, `screens/DashboardScreen.tsx`,
  `components/playback/{OverlayLayer,PlaybackBarCard,AssetDropdown,VideoHeader}.tsx`,
  `components/upload/ParameterModal.tsx`, `styles/components.css`.

## Acceptance
- Subsea (defect) renders exactly as before; pylon-like inventory mode shows component counts, no flags/work-orders, and no dimming. `tsc` + `vite build` pass.

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — inventory-mode branching, all-primary dimming.
- [[DR-011-LOD-Density-Cluster-Timeline]] — the bar; dimming layer inert when all classes primary.
- [[DR-007-Tracked-Instances-Over-Per-Frame-Detection]] — a component is a tracked instance.
