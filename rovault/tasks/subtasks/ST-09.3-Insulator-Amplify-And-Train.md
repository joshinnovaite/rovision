# ST-09.3: Insulator Amplify + Train

Parent: [[BOW-09-Insulators-Domain-Pipeline]]
Status: done
Progress: **DONE — mAP50 0.994, `cls_loss` ~0.5.** The repeated zero-mAP collapse was NOT object shape: it
was a **leaked global bf16 autocast** (SAM 2 setup) corrupting YOLO's EMA validation weights
([[DR-015-Leaked-Global-Autocast-Corrupted-Detector-Training]]). Fix = wrap the §16 training call in
`with torch.autocast('cuda', enabled=False):`. The diagnostic chain that cracked it: visualised loaded
labels (correct) → re-ran proven subsea recipe (still failed) → ran base `yolo11n.pt` (detected workers as
COCO `person`) → noticed easy `worker` class *also* failing to exactly 0 (disconfirmed object-shape) →
found the leaked autocast in cell 4 + corroborating Ultralytics 8.4.63–8.4.66 EMA/validation changelog.

## Preconditions
- v2 golden set `labels_insulators.json` exists ([[ST-09.2-Insulator-Golden-Set]]) — `insulator_string`+`worker`, 9 maw clips.
- Notebook is already domain-aware (single `DOMAIN` switch, registry class list, per-domain Drive paths — carried from [[BOW-08-Pylon-ML-Pipeline]]); set `DOMAIN='insulators'`.
- Colab GPU runtime + Drive persistence available ([[DR-006-Drive-Persistence-And-Resumable-Batch]]).
- SAM 2-segmentability of the targets confirmed up front (the v1 lesson — [[DR-014-Domain-Viability-Gated-By-Object-Scale]]).

## Steps
1. Set `DOMAIN='insulators'` in the notebook install cell; confirm the class list resolves from the registry and Drive paths suffix correctly.
2. Amplify the v2 seeds via SAM 2 propagation; spot-check amplified-box overlay across consecutive frames for stable propagation (the diagnostic that exposed v1).
3. Train a **separate** YOLO11n on the insulators data (overfit-by-design — domains never share a detector).
4. Run the diagnostic playbook to confirm the detector actually learns: non-zero mAP, confident detections on training frames at low conf, `cls_loss` falling (not plateauing ~2–3).

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` (§13–§16, domain-aware), Drive `MyDrive/rovision/insulators/`.

## Acceptance
- An insulators YOLO11n weight file is produced; **non-zero mAP** and qualitatively sane detections on insulator footage. (v1 failed this; v2 in flight.)

## References
- [[DR-015-Leaked-Global-Autocast-Corrupted-Detector-Training]] — the true root cause + the fix.
- [[DR-014-Domain-Viability-Gated-By-Object-Scale]] — the superseded object-shape hypothesis (cautionary record).
- [[domain-viability-and-detector-diagnostics]] — the ordered diagnostic playbook + failure signature.
- [[DR-005-Overfit-By-Design-PoC-Scope]] — each domain its own overfit model.
- [[DR-002-SAM2-Propagation-For-Label-Amplification]] — the amplification step (source of v1's corruption).
- [[label-amplification]] — the amplification workflow.
- [[subsea_defect_demo.ipynb]] — the domain-aware notebook.
