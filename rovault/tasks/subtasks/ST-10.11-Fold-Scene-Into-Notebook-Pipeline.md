# ST-10.11: Fold Scene Into the Notebook Pipeline (Auto-Emit Into §17 meta.json)

Parent: [[BOW-10-E1-Phase-0-Proof-Of-Capability]]
Status: not-started
Progress: not-started — **open pipeline-hygiene follow-up.** The 5 current bundles were patched locally
(scene block written into meta.json after the fact, via the §18b calibration sweep). This item makes
future clips self-populate instead of being hand-patched. Phase 0 is functionally delivered; this one item
keeps BOW-10 ticked-open.

## Preconditions
- Calibrated thresholds exist ([[ST-10.9-S18b-Calibration-Sweep]]).
- §18b is currently a local sweep, separate from the §17 bundle export.

## Steps
1. Carry the calibrated sky/water thresholds in §18 (constants the sweep validated).
2. Have the scene measurement run as part of the §17 demo/export path for `DOMAIN=='e1'`.
3. Auto-emit the `scene: {sky_state, water_state, method, metrics}` block into the §17 bundle `meta.json`,
   so a new clip self-populates its scene badge with no local patching.
4. (Optional) Carry forward the deferred region-selection improvement for the chop scale — subtract the
   YOLO boat/spray boxes, take the largest water-coloured connected component, robust median over more
   frames — if it lands by then.

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` — §18 (thresholds) + §17 (auto-emit scene into meta.json).

## Acceptance
- A freshly exported E1 §17 bundle contains a populated `scene` block in `meta.json` with no manual step.

## References
- [[DR-017-Segment-Then-Measure-Scene-Attributes]] — the technique being folded into the pipeline.
- [[DR-019-Scene-Attribute-Aggregate-Badge-Surface]] — the meta.json `scene` contract this auto-emits.
- [[subsea_defect_demo.ipynb]] — §17/§18.
