# ST-10.3: DR-015 Autocast Guard Verified in §16

Parent: [[BOW-10-E1-Phase-0-Proof-Of-Capability]]
Status: done
Progress: done — confirmed `with torch.autocast('cuda', enabled=False):` already wraps the §16 YOLO
training call. Pre-flight for the eventual E1 train: the leaked-autocast zero-mAP collapse
([[DR-015-Leaked-Global-Autocast-Corrupted-Detector-Training]]) cannot recur for E1.

## Preconditions
- §16 training runs inside a session that leaks a global bf16 autocast from §2 (SAM 2 setup); the guard is
  the fix established in [[DR-015-Leaked-Global-Autocast-Corrupted-Detector-Training]].

## Steps
1. Inspect §16 of [[subsea_defect_demo.ipynb]] for the autocast guard around the training call.
2. Confirm it is present and correct (no E1-specific change needed).

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` — §16 training cell.

## Acceptance
- The §16 training call is shielded from the leaked global autocast; E1 training inherits the protection.

## References
- [[DR-015-Leaked-Global-Autocast-Corrupted-Detector-Training]] — the guard this verifies.
- [[DR-018-E1-Phase-0-Phase-1-Split]] — E1 training inherits this pre-flight.
- [[subsea_defect_demo.ipynb]] — §16.
