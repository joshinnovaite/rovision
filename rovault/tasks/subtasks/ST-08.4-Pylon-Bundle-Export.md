# ST-08.4: Pylon Bundle Export

Parent: [[BOW-08-Pylon-ML-Pipeline]]
Status: abandoned
Progress: ABANDONED with parent [[BOW-08-Pylon-ML-Pipeline]] — never reached export; gated on a trained
pylon detector that never materialised (mAP 0). The §17 domain-aware export plumbing (`"domain": DOMAIN`
stamp, per-domain model/output dir, hero-clip default) is sound and **carries forward** to the insulators
domain. Superseded by [[BOW-09-Insulators-Domain-Pipeline]]; lesson in
[[DR-014-Domain-Viability-Gated-By-Object-Scale]]. Preserved as a record.

## Preconditions
- A trained pylon detector exists ([[ST-08.3-Pylon-Amplify-And-Train]]).
- The §17 bundle export contract is known ([[BOW-01-Notebook-Export-Extension]]); ingest reads `meta.domain` ([[ST-07.2-Backend-Domain-Plumbing]]).

## Steps
1. Run the §17 detect→segment export on a pylon clip with the pylon detector. **[pending]** — Colab GPU run; cell defaults the hero clip to the "Aerial Drone: Electricity Pylons" video.
2. Write `"domain":"pylon"` into the bundle `meta.json` so ingest tags the row's domain. **[done]** — cell 37 stamps `"domain": DOMAIN`; per-domain model/output dir; `ASSET='generic'` for pylon.
3. Provide a seed bundle so the inventory-mode app ([[BOW-07-Multi-Domain-App-Architecture]]) has pylon content to display end-to-end. **[pending]** — emit in Colab, then seed into `app/backend/data/bundles/`.

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` (§17 export), a pylon bundle (gitignored / seed).

## Acceptance
- A pylon bundle ingests with `domain='pylon'`; the app shows it under the pylon toggle in inventory mode (component counts, no flags).

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — `meta.domain` field + inventory display.
- [[BOW-01-Notebook-Export-Extension]] — the bundle export this extends.
- [[subsea_defect_demo.ipynb]] — the §17 export section.
