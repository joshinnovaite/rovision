# ST-08.1: Pylon Footage + Class Proposal

Parent: [[BOW-08-Pylon-ML-Pipeline]]
Status: abandoned
Progress: ABANDONED with parent [[BOW-08-Pylon-ML-Pipeline]] — the extract-only review judged class
*presence* but not detector *scale viability*, which is the axis that later sank the domain (objects too
small/distant/nested). The 5-class set was locked but the footage was unviable. Superseded by the
insulators domain ([[BOW-09-Insulators-Domain-Pipeline]]); lesson in
[[DR-014-Domain-Viability-Gated-By-Object-Scale]]. Preserved as a record.

## Outcome — confirmed pylon taxonomy
Final 5 classes: **pylon_body, cross_arm, pylon_top, insulator, foundation**.

Refinements from the provisional list (DR-013's anticipated proposal):
- **Dropped `vibration_damper`** — Stockbridge dampers are not resolvable at drone range; zero labelable instances in the footage.
- **Added `cross_arm`** — the most distinctive and consistently-visible sub-component across all four clips.

Per-class signal strength (informs amplification/labelling effort downstream): `pylon_body` is the strongest signal; `cross_arm` is the most consistently visible; `foundation` appears in 3 of 4 clips (legs splaying from the ground); `insulator` is the weakest (small, only resolvable in close frames).

Footage note: the strongest "hero" clip is the "Aerial Drone: Electricity Pylons" video (lattice towers in every frame); one clip (sunset) shows a visually distinct red/white painted tower. This is the confirmation of the provisional taxonomy that [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] already anticipated — no new DR.

## Preconditions
- Pylon footage is available to add to the repo's local scratch (`test_footage_pylon/`).
- The registry `pylon` block exists, seeded with a **provisional** class list (5 classes from the user's initial proposal) ([[ST-07.1-Domain-Registry-domains-json]]).

## Steps
1. Add pylon footage to `test_footage_pylon/` (gitignored, local-only).
2. Run the labeller in `--extract-only` mode to dump review frames.
3. Eyeball the frames; confirm/refine the **provisional** pylon component class list (insulator, foundation, pylon_body, pylon_top, vibration_damper) against what the actual footage shows.
4. Lock the confirmed/refined class list into the `pylon` block of `rovision/domains.json` (mode `inventory`, `defect_classes` empty), updating the provisional seed as needed.

## Files
- `test_footage_pylon/` (local, 4 drone clips), `rovision/domains.json` (the `pylon` block).

## Acceptance
- A reviewed, agreed pylon component class list is committed to the registry's `pylon` block.

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — pylon = inventory; registry is the class source.
- [[golden-set-labelling]] — the extract-only review workflow reused here.
- [[label_defects.py]] — the extract-only tool.
