# ST-10.1: E1 Domain Registration in domains.json

Parent: [[BOW-10-E1-Phase-0-Proof-Of-Capability]]
Status: done
Progress: done — `e1` block added to `rovision/domains.json`: `mode: inventory`, classes `race_boat`,
`turn_buoy`, `spray_plume`, `defect_classes: []`, `severity: null`, per-class colours. Mirrors the
`insulators` block; zero architecture change ([[DR-016-E1-Domain-As-Inventory-Mode-Phase-0]]).

## Preconditions
- The domain registry + `mode` framework exist ([[DR-013-Multi-Domain-Registry-And-Inventory-Mode]],
  carried through [[BOW-09-Insulators-Domain-Pipeline]]).
- E1 deliberately registered as `inventory`, **not** a new scene/context mode (deferred to Phase 1).

## Steps
1. Add an `e1` block to `rovision/domains.json` keyed like `subsea`/`insulators`.
2. Set `mode: inventory`, `defect_classes: []`, `severity: null`.
3. Define classes `race_boat`, `turn_buoy`, `spray_plume` + `all_classes` + per-class colours.

## Files
- `rovision/domains.json` — the new `e1` block.

## Acceptance
- E1 selectable as a domain; inventory rails (counts, no flags/severity) render with zero code branching.

## References
- [[DR-016-E1-Domain-As-Inventory-Mode-Phase-0]] — the registration decision.
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — registry + inventory mode reused.
- [[BOW-09-Insulators-Domain-Pipeline]] — the inventory-block precedent mirrored.
