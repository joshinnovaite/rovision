# ST-10.6: E1 Hero-Clip §17 Demo Branch

Parent: [[BOW-10-E1-Phase-0-Proof-Of-Capability]]
Status: done
Progress: done — §17 `e1` branch added; ran the detect→segment composite over the Como clips and exported
self-contained `domain=e1` bundles (5 clips, 53 tracks total) ingestible by the multi-domain app.

## Preconditions
- E1 clips staged in `test_footage_e1/` ([[ST-10.5-E1-Footage-Capture]]).
- The §17 demo/export cell is already domain-aware (carried from [[BOW-09-Insulators-Domain-Pipeline]]).

## Steps
1. Add an `e1` branch to the §17 demo cell selecting an E1 hero clip from `test_footage_e1/`.
2. Run the detect→segment composite over it for the E1 classes (and surface §18 scene attributes for the
   clip if convenient).
3. Export a self-contained inference bundle with `domain=e1` (mirroring the insulators export path).

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` — §17 demo/export cell, new `e1` branch.

## Acceptance
- §17 produces an E1 hero-clip bundle (`domain=e1`) ingestible by the multi-domain app.

## References
- [[DR-016-E1-Domain-As-Inventory-Mode-Phase-0]] — bundles carry `domain=e1`, inventory mode.
- [[DR-007-Tracked-Instances-Over-Per-Frame-Detection]] — the tracked-instance demo path reused.
- [[BOW-09-Insulators-Domain-Pipeline]] — the §17 export precedent.
- [[subsea_defect_demo.ipynb]] — §17.
