# ST-11.2: Orba Live Runtime Values

Parent: [[BOW-11-Orba-CMMS-Service-Request-Integration]]
Status: not-started
Progress: blocked — awaiting three runtime values from the Orba developer; the code path (ST-11.1) is done and stub-verified.

## Preconditions
- [[ST-11.1-Orba-Backend-Proxy-And-Send-Button]] done (code path complete, returns 503 until env set).
- Three values **owed by the Orba developer**: a live `ORBA_BASE_URL`, a valid BEDFORD
  `ORBA_ASSETNUM`, and the shared `ORBA_INGEST_SECRET`.

## Steps
1. Receive the three runtime values from the Orba developer.
2. Populate `app/backend/.env` (gitignored) with `ORBA_BASE_URL`, `ORBA_ASSETNUM`, `ORBA_INGEST_SECRET`.
3. Restart the backend and confirm the route no longer 503s.
4. Run one finding through to live Orba; confirm an `SR-####` is created and the dedup guard disables the button.

## Files
- `app/backend/.env` — the three live values (gitignored; not committed).

## Acceptance
- A "Send to Orba" click against live Orba creates real Service Requests (one per finding) at site BEDFORD.

## References
- [[DR-020-Orba-CMMS-Integration-Server-Side-Proxy]] — the integration decision and the gated live-demo note.
- [[ST-11.1-Orba-Backend-Proxy-And-Send-Button]] — the code path this unblocks.
