# ST-08.3: Pylon Amplify + Train

Parent: [[BOW-08-Pylon-ML-Pipeline]]
Status: abandoned
Progress: ABANDONED with parent [[BOW-08-Pylon-ML-Pipeline]]. The domain-aware notebook plumbing (single
`DOMAIN` switch, registry-driven class list, per-domain Drive paths) is sound and **carries forward** to
the insulators domain. The pylon amplify+train was actually run on Colab across multiple attempts and the
detector never learned (mAP 0) — the footage was unviable, not the recipe. Superseded by
[[BOW-09-Insulators-Domain-Pipeline]]; lesson in [[DR-014-Domain-Viability-Gated-By-Object-Scale]].
Preserved as a record.

## Preconditions
- Pylon golden set `labels_pylon.json` exists ([[ST-08.2-Labeller-Domain-Flag-And-Pylon-Golden-Set]]).
- Colab GPU runtime + Drive persistence available ([[DR-006-Drive-Persistence-And-Resumable-Batch]]).

## Steps
1. Make the §13/§14 amplification + §15/§16 train sections domain-aware (read the `pylon` class list from the registry). **[done]** — single `DOMAIN` install-cell switch; registry-driven class list; per-domain `_pylon`-suffixed Drive paths across cells 31/33/35; `DOMAIN='subsea'` reproduces original paths.
2. Amplify the pylon seeds via SAM 2 propagation into a pylon amplified dataset. **[pending]** — Colab GPU run.
3. Train a **separate** YOLO11n on the pylon data (overfit-by-design — domains never share a detector). **[pending]** — Colab GPU run.

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` (§13–§16, domain-aware), Drive `MyDrive/rovision/pylon/`.

## Acceptance
- A pylon YOLO11n weight file is produced from the pylon amplified dataset; qualitatively sane on pylon footage.

## References
- [[DR-005-Overfit-By-Design-PoC-Scope]] — each domain its own overfit model.
- [[DR-002-SAM2-Propagation-For-Label-Amplification]] — the amplification step.
- [[label-amplification]] — the amplification workflow.
- [[subsea_defect_demo.ipynb]] — the notebook gaining domain-awareness.
