# ST-09.4: Insulator Bundle Export

Parent: [[BOW-09-Insulators-Domain-Pipeline]]
Status: done
Progress: **DONE** — two bundles (`maw_3`, `maw_5`) exported with `domain=insulators` and seeded into
`app/backend/data/bundles/`; both show under the "Pylon Insulators" toggle in inventory mode (component
counts, no flags).

## Preconditions
- A trained insulators detector exists ([[ST-09.3-Insulator-Amplify-And-Train]]).
- §17 export already stamps `"domain": DOMAIN` into `meta.json` and defaults the hero clip (carried from [[BOW-08-Pylon-ML-Pipeline]]); the hero clip is set to the insulator drone clip.
- Ingest reads `meta.domain` ([[ST-07.2-Backend-Domain-Plumbing]]).

## Steps
1. Run the §17 detect→segment export on an insulator clip with the insulators detector (`DOMAIN='insulators'`, hero clip = insulator drone clip).
2. Confirm `meta.json` carries `"domain":"insulators"` so ingest tags the row's domain.
3. Seed the emitted bundle into `app/backend/data/bundles/` so the inventory-mode app ([[BOW-07-Multi-Domain-App-Architecture]]) shows it end-to-end (component counts, no flags).

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` (§17 export), an insulators bundle (gitignored / seed), `app/backend/data/bundles/`.

## Acceptance
- An insulators bundle ingests with `domain='insulators'`; the app shows it under the insulators toggle in inventory mode.

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — `meta.domain` field + inventory display.
- [[BOW-01-Notebook-Export-Extension]] — the bundle export this extends.
- [[subsea_defect_demo.ipynb]] — the §17 export section.
