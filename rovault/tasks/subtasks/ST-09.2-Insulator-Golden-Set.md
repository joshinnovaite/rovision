# ST-09.2: Insulator Golden Set

Parent: [[BOW-09-Insulators-Domain-Pipeline]]
Status: done
Progress: done — **redone for v2** after v1 failed. v1 (3 clips, `insulator_string`+`conductor`, ~64
boxes) produced a training set corrupted by unstable SAM 2 masks. v2 golden set re-labelled over **9 short
single-pan clips (maw_1–maw_9)** for the 2-class set `insulator_string`+`worker`: insulator_string 167
boxes / 27 frames, worker 57 boxes / 26 frames. `conductor` dropped (thin/elongated). See
[[domain-viability-and-detector-diagnostics]].

## Preconditions
- Insulators taxonomy locked into the registry ([[ST-09.1-Insulator-Footage-Scan-And-Taxonomy]]) — now v2: `insulator_string`+`worker` in `rovision/domains.json`.
- Labeller `--domain` flag already reads the registry class list / footage dir / output path (carried from [[BOW-08-Pylon-ML-Pipeline]]).
- v2 footage: 9 short single-pan clips (maw_1–maw_9) chosen for SAM 2-segmentability, replacing v1's 3 clips.

## Steps
1. Curate/clear `labels_insulators.json` before re-labelling — the labeller **appends**, so v1 labels must be cleared to avoid orphaned cross-pass records (hit during the v1→v2 swap).
2. Run `label_defects.py --domain insulators` (multi-pass) over the 9 maw clips.
3. Hand-draw boxes for `insulator_string` (hero) and `worker` (hi-vis linemen).
4. Sanity-check for degenerate boxes; confirm output writes to `labels_insulators.json`.

## Files
- `rovision/labelling/label_defects.py` (unchanged; uses `--domain`), `rovision/labelling/labels_insulators.json` (v2 golden set, in git).

## Acceptance
- `labels_insulators.json` holds tight boxes for `insulator_string`+`worker` across the 9 maw clips; subsea labelling unaffected. **Met.**

## References
- [[DR-013-Multi-Domain-Registry-And-Inventory-Mode]] — labeller reads the registry.
- [[DR-014-Domain-Viability-Gated-By-Object-Scale]] — why v2 dropped `conductor` and changed footage.
- [[domain-viability-and-detector-diagnostics]] — footage curation + the append-gotcha.
- [[GoldenSet]] — the per-domain golden set this extends.
- [[golden-set-labelling]] — the labelling workflow.
- [[label_defects.py]] — the labeller (already domain-aware).
