# masklet

SAM 2's term for a single object's mask tracked across video frames, produced by propagating a prompt through the clip. It is the unit that [[label amplification]] harvests — one box per frame is read off each masklet. See [[sam2-as-engine]].
