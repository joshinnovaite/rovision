# golden set

The small, human-verified set of seed labels (boxes + classes on sampled frames) that seeds the whole pipeline. Produced in [[golden-set-labelling]] and stored as the [[GoldenSet]]. It is the starting point that [[label amplification]] multiplies into training data.
