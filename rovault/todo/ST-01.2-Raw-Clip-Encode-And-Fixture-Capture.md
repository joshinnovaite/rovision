# ST-01.2: Raw-Clip Encode And Fixture Capture

Parent: [[BOW-01-Notebook-Export-Extension]]
Status: done
Progress: raw.mp4 (faststart) captured in the dam_trash_rack bundle; seeks correctly via the backend (206).
Progress: raw.mp4 faststart encode (between Pass 1 and Pass 2) + overlay faststart added to the §17 cell. Remaining: run §17 on Colab and capture the fixture bundle.

## Preconditions
- [[ST-01.1-Polygons-Meta-And-Clip-Local-Reindex]] done: §17 emits `meta.json` + polygon-bearing
  `detection_cards.json` + `track_cards.json`, clip-local 0-based, with the `n_frames` assert.
- Pass 1 has buffered raw frames to `TMP/%05d.jpg`; Pass 2 (overlay) overwrites those same files.

## Steps
1. **Between Pass 1 and Pass 2** (before overlay overwrites `TMP`), encode `raw.mp4` from
   `TMP/%05d.jpg` with H.264 baseline profile, `yuv420p`, and `-movflags +faststart`.
2. Use `enc_fps` (= `round(fps)`) as the ffmpeg input/output frame rate so time↔frame stays consistent.
3. Add `-movflags +faststart` to the **existing overlay encode** (`internal_assets_demo.mp4`) too.
4. Assemble the bundle directory: `meta.json`, `detection_cards.json`, `track_cards.json`,
   `raw.mp4`, `internal_assets_demo.mp4` (5 files).
5. Re-run §17 end-to-end on a hero clip (e.g. `dam_trash_rack_inspection`) to produce a real bundle.
6. Verify `raw.mp4` plays and **seeks** in a browser (faststart moves moov atom to the front).
7. Capture this bundle as the committed frontend **fixture** (a small clip is acceptable for the fixture).
8. Note the exact `raw.mp4` bytes: the demo user must upload *this same file* or the SHA-256 dedup misses.

## Files
- `rovision/notebooks/subsea_defect_demo.ipynb` — §17 cell: insert raw-clip ffmpeg encode between
  the two passes; add `+faststart` to the overlay encode. Bundle dir layout produced by the cell.

## Acceptance
- Bundle directory contains all **5 files**; `raw.mp4` is faststart-encoded and seeks in a browser.
- `enc_fps` in `meta.json` equals the ffmpeg rate actually used (no fps/enc_fps drift).
- A fixture bundle is captured for frontend dev (committed per ST-03.2).

## References
- [[BOW-01-Notebook-Export-Extension]] — parent body of work.
- [[ST-01.1-Polygons-Meta-And-Clip-Local-Reindex]] — precondition (meta + cards).
- [[ST-02.2-Ingest-And-Rollup]] — consumes the `raw.mp4` bytes for hashing.
- [[ST-03.2-Types-API-Client-Fixture]] — commits this bundle as the fixture.
- [[InferenceOutputs]] — bundle JSON schema.
